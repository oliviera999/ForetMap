# ForetMap — copier vers .env pour le dev local avec Docker (voir docs/LOCAL_DEV.md)
# Conteneur : MariaDB 11.4 (image mariadb:11.4.10). Ne pas utiliser ces identifiants en production (o2switch).

DB_HOST=127.0.0.1
DB_PORT=3306
# Si 3306 est déjà utilisé sur la machine : DB_PORT=3307 et, pour Docker Compose, la même ligne :
# FORETMAP_DB_PUBLISH_PORT=3307
DB_NAME=foretmap_local
DB_USER=root
DB_PASS=foretmap_local_root

PORT=3000
NODE_ENV=development

JWT_SECRET=dev_jwt_secret_local_only

# Optionnel : CORS proche prod (sinon CORS permissif en dev)
# FRONTEND_ORIGIN=http://localhost:3000

# Plafond du limiteur d'IP des routes d'authentification (tentatives / 15 min, défaut 60).
# La force brute est contrée par compte (lib/loginThrottle.js) ; ce plafond borne le volume global.
# FORETMAP_AUTH_RATE_LIMIT_PER_15MIN=60

# Lien Moodle (docs/AUDIT_MOODLE_IDENTITES_2026-09.md ; mise en service : docs/EXPLOITATION.md).
# Le jeton Web Services reste dans ce fichier : jamais en base, jamais dans un réglage admin.
# Sans ces deux variables, les routes /api/admin/integrations/moodle/* répondent 503.
# MOODLE_BASE_URL=https://moodle.olution.info
# MOODLE_WS_TOKEN=
# MOODLE_WS_TIMEOUT_MS=20000
# Interrupteur d'urgence : 0 coupe toutes les routes Moodle et le cron sans toucher au jeton.
# MOODLE_SYNC_ENABLED=1
# Entrée depuis le cours (LTI 1.3, lot M6). Jamais en base, jamais un réglage admin.
# Sans ces variables, /api/lti/* répond 503. Voir docs/EXPLOITATION.md.
# LTI_ISSUER=https://moodle.olution.info
# LTI_CLIENT_ID=
# LTI_DEPLOYMENT_ID=
# LTI_PLATFORM_AUTH_URL=https://moodle.olution.info/mod/lti/auth.php
# LTI_PLATFORM_JWKS_URL=https://moodle.olution.info/mod/lti/certs.php
# Clé privée PKCS8 de l'outil (PEM, retours à la ligne conservés entre guillemets).
# LTI_TOOL_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# LTI_TOOL_KID=foretmap-lti
# Origine G&L pour le dépôt de session après lancement (sinon dérivée de l'origine ForetMap).
# GL_FRONTEND_ORIGIN=https://gl.exemple.fr
