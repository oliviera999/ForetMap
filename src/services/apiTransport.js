/**
 * Ré-export de compatibilité : le module a été promu dans `src/shared/apiTransport.js`
 * (consommé par ForetMap ET GL). Importer directement depuis `src/shared/` dans le nouveau code.
 */
export * from '../shared/apiTransport.js';
