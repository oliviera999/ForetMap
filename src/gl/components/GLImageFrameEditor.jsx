/**
 * Éditeur de cadre d'image — **alias historique** G&L.
 *
 * L'outil n'avait rien de propre au jeu : il vit désormais dans le socle partagé
 * (`src/shared/image-frame/ImageFrameEditor.jsx`, lot 7 du plan de convergence) et sert aussi
 * aux photos de lieux ForetMap. Les classes CSS et le comportement sont inchangés.
 */
export { ImageFrameEditor as GLImageFrameEditor } from '../../shared/image-frame/ImageFrameEditor.jsx';
